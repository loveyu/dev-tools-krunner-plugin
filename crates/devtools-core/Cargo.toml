[package]
name = "devtools-core"
version = "0.1.0"
edition = "2021"
description = "Shared models and protocols for DevTools."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
